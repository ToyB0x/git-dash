name: deploy
on: push

permissions:
  contents: read # for checkout repository

jobs:
  deploy-dev:
    uses: ToyB0x/git-dash/.github/workflows/deploy-base.yml@feat/deploy-ci
    with:
      environment: development
      secrets: inherit
